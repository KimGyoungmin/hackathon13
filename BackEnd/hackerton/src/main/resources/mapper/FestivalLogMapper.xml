<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.hackerton.mapper.FestivalLogMapper">

    <!-- FestivalLog ResultMap -->
    <resultMap id="FestivalLogResultMap" type="com.example.hackerton.domain.FestivalLog">
        <id property="logId" column="log_id"/>
        <result property="festivalId" column="festival_id"/>
        <result property="promoIncrease" column="promo_increase"/>
        <result property="budgetIncrease" column="budget_increase"/>
        <result property="promoIncreaseRate" column="promo_increase_rate"/>
        <result property="budgetIncreaseRate" column="budget_increase_rate"/>
        <result property="evidenceSummary" column="evidence_summary"/>
        <result property="forecastRevenue" column="forecast_revenue"/>
        <result property="forecastVisitors" column="forecast_visitors"/>
        <result property="createDt" column="create_dt"/>
        <result property="deleteYn" column="del_yn"/>
    </resultMap>

    <!-- 모든 축제 로그 조회 -->
    <select id="findAll" resultMap="FestivalLogResultMap">
        SELECT *
        FROM festival_log
        WHERE del_yn = 'N'
        ORDER BY create_dt DESC
    </select>

    <!-- 로그 ID로 조회 -->
    <select id="findById" parameterType="Long" resultMap="FestivalLogResultMap">
        SELECT *
        FROM festival_log
        WHERE log_id = #{logId} AND del_yn = 'N'
    </select>

    <!-- 축제 ID로 로그 조회 -->
    <select id="findByFestivalId" parameterType="Long" resultMap="FestivalLogResultMap">
        SELECT *
        FROM festival_log
        WHERE festival_id = #{festivalId} AND del_yn = 'N'
        ORDER BY create_dt DESC
    </select>

    <!-- 최근 예측 로그 조회 -->
    <select id="findRecentPredictions" resultMap="FestivalLogResultMap">
        SELECT *
        FROM festival_log
        WHERE del_yn = 'N'
        ORDER BY create_dt DESC
        LIMIT 10
    </select>

    <!-- 축제 로그 등록 -->
    <insert id="insert" parameterType="com.example.hackerton.domain.FestivalLog" 
            useGeneratedKeys="true" keyProperty="logId">
        INSERT INTO festival_log (
            festival_id, promo_increase, budget_increase, promo_increase_rate, budget_increase_rate,
            evidence_summary, forecast_revenue, forecast_visitors, create_dt, del_yn
        ) VALUES (
            #{festivalId}, #{promoIncrease}, #{budgetIncrease}, #{promoIncreaseRate}, #{budgetIncreaseRate},
            #{evidenceSummary}, #{forecastRevenue}, #{forecastVisitors}, #{createDt}, 'N'
        )
    </insert>

    <!-- 축제 로그 수정 -->
    <update id="update" parameterType="com.example.hackerton.domain.FestivalLog">
        UPDATE festival_log 
        SET promo_increase = #{promoIncrease},
            budget_increase = #{budgetIncrease},
            promo_increase_rate = #{promoIncreaseRate},
            budget_increase_rate = #{budgetIncreaseRate},
            evidence_summary = #{evidenceSummary},
            forecast_revenue = #{forecastRevenue},
            forecast_visitors = #{forecastVisitors}
        WHERE log_id = #{logId}
    </update>

    <!-- 축제 로그 물리적 삭제 -->
    <delete id="delete" parameterType="Long">
        DELETE FROM festival_log WHERE log_id = #{logId}
    </delete>

    <!-- 축제 로그 논리적 삭제 -->
    <update id="softDelete" parameterType="Long">
        UPDATE festival_log 
        SET del_yn = 'Y'
        WHERE log_id = #{logId}
    </update>

</mapper>
